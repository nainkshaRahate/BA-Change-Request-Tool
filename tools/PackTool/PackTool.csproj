<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Build: dotnet run - pack -i CRTool -o CRTool_v1.msapp (from repo root) -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.PowerPlatform.Formulas.Tools" Version="0.8.2733.113-preview" />
  </ItemGroup>

</Project>
